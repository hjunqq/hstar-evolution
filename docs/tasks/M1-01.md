# 任务 M1-01：首条静力路径的 reader/调用/文件/游标表

- 状态：IN_REVIEW
- 实现负责人：Huijun（Claude 辅助）
- 复核人：待指定
- 前置任务/接受证据：M0（`f7abe9d`，IN_REVIEW）
- 工作分支/起始提交：`main` / `f7abe9d`
- 能力组合与明确排除项：static_2d 切片；只登记，不修改 `legacy/yl`，不改 release 构建

## 问题与预期行为

M1-02 要包装的读取点必须先被枚举并证明确实在路径上执行；M2-01 需要每个字段的目标对象。
预期：每个已执行读取点有稳定 ID、字段契约、阶段与消费者；未执行候选可见并有 guard 理由。

## 变更边界

新增 `tools/yl_io_inventory.py`、`tools/yl_io_trace.sh`、`tools/build.sh` 的 `trace` profile、
`docs/m1/{io-sites.json,reader-inventory.toml,reader-inventory.md,reader-inventory.narrative.md,evidence/}`。

## 验证计划

| 测试 ID | 输入/模式 | 判据 | 预期关系 | 证据位置 |
|---|---|---|---|---|
| M1-01-a | gdb 追踪两例 | 追踪运行的 `1.flavia.res` 与 reference 逐值相同 | 相同 | `evidence/*/compare-vs-reference.json` |
| M1-01-b | `yl_io_inventory.py check` | ID 唯一；锚点与源码一致；每个命中 read/open/rewind 均已登记；每条登记有两例证据；`state_target/phase/format` 词表合法；每个普查单元要么已登记要么在 not_on_path | PASS | 本文件 |
| M1-01-c | 两例位点集合 | 相同 | 相同 | `evidence/*/hits.json` |
| 数值门 | — | 不适用 | NOT_APPLICABLE | — |

## 完成标准与回退

`check` PASS；叙述文档含方法、时间线、盲区。回退：删除 `docs/m1/` 与两个工具，`build.sh` 去掉 `trace`。

## 实际结果

- 2026-09-07：静态全集 1022 处；两例各命中 211 位点（集合相同），read 152（149 执行 + 3 reached_only），
  open 46，rewind 13；追踪运行结果与 reference 逐字节相同；`check` PASS。
- 纠正了两个先前假设：`.man` 由 `STATIC_U` 读取（`back_analysis` 未命中）；`.sol` 在 `PROFILE`
  内延迟读取（solver_lazy）。主程序 `.man` 预扫为 0 次循环。
- 工具缺陷修复记录：初版扫描用 `splitlines()` 导致 Fem.f90 行号偏移（0x85 字节）；初版未覆盖
  单行 `if (...) read` 形式（59 处）。两者修正后重跑，证据以修正版为准。
- 未覆盖：guard 分支只有一个样本；list-directed 跨记录行为未逐条核对；`consumers` 为人工追踪。

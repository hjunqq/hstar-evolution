# 任务 M0-04（a/b/c）：结果解析器、两例参考结果、脏树对照

- 状态：IN_REVIEW
- 实现负责人：Huijun（Claude 辅助）
- 复核人：待指定
- 前置任务/接受证据：M0-03（本轮同批提交）、M0-05a（`c0faa44`）
- 工作分支/起始提交：`main` / `c0faa44`
- 能力组合与明确排除项：2D Q4 线弹性静力、PROFILE、重力体力、单增量；不作物理正确性结论

## 问题与预期行为

需要冻结“旧 YL 在固定环境下做了什么”。预期：`1.flavia.res` 可结构化解析；单线程三次运行
逐值相等；脏树修改对本切片是否有影响有机器判定。

## 变更边界

新增 `tools/yl_compare.py`；新增 `cases/golden/static_2d/*/reference/`（`results.json`、
`run-manifest-{1,2,3}.json`、`build-manifest.json`、`repeat-report.json`）；
`docs/m0/evidence/candidate/`。不修改输入、源码、容差。

## 验证计划

| 测试 ID | 输入/模式 | 判据 | 预期关系 | 证据位置 |
|---|---|---|---|---|
| M0-04a | release 冒烟输出 | 块结构、节点数=NPOIN、有限、无重复 ID | PASS | `reference/repeat-report.json` |
| B02 | release，`OMP/MKL_NUM_THREADS=1`，两例各 3 次 | 全部 COMPLETED；解析后逐值精确相等（atol=rtol=0） | PASS | `reference/repeat-report.json`、`run-manifest-*.json` |
| S02 前置（比较器反例） | 删块、删/加节点、扰动 1e-7、NaN、改分量名、改列数 | 全部 FAIL；精确副本 PASS | 7 FAIL + 1 PASS | 本文件 |
| M0-04c | 脏树 candidate 二进制两例 | COMPLETED 且与 reference 逐值相等 | 相同 → VERIFY 项改 DEFER | `docs/m0/evidence/candidate/` |
| 性能登记 | 同上 | 墙钟与 `ru_maxrss` 入 manifest | 登记 | `reference/run-manifest-*.json` |

## 完成标准与回退

reference 目录完整、哈希可复现；`repeat-report.json` verdict=PASS；04c 判定写入
`docs/m0/orig-worktree-diff.md`。回退：删除 reference 目录，`cases/manifest.toml` 状态回到 `inputs-hashed`。

## 实际结果

- 2026-09-07 M0-04a PASS：DISPLACEMENT（vector，2 分量）与 STRESS（matrix，SIGXX/SIGYY/SIGXY/SIGZZ）
  两块，节点数 289 / 81，全部有限；`1.flavia.msh` 为 0 字节，`.gpv` 含高斯点应力（未纳入观察量）。
- B02 PASS：两例各 3 次 COMPLETED，`1.flavia.res` SHA-256 三次相同，逐值最大差 0；
  墙钟 0.03 s；峰值 RSS 27.5 MB（Cook）/ 24.9 MB（Lamé）。位移最大值 7.57e-3 m（Cook）、
  应力最大 4.00e6 Pa（Cook）、Lamé 最大值 1.47e5（应力）。
- 比较器反例 7/7 FAIL，精确副本 PASS；扰动 1e-7 在临时 cross_path 容差（rtol 1e-9）下 FAIL、
  在 rtol 1e-6 下 PASS，说明容差节被正确加载。
- M0-04c：candidate（脏树，SHA-256 `dea1a66d…`）两例 COMPLETED，与 reference 逐值精确相等。
  3 个 VERIFY hunk 改判 DEFER；`factw` 初始化并入 M1-03。
- 非正式对照：reference `1.flavia.res` 与来源仓库 2026-09-06 的历史输出逐字节相同。
- 未覆盖：`.gpv`、`.chk` 未解析；无反力；容差定稿仍待独立任务；未做多线程或其他机器复现。

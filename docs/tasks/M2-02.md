# 任务 M2-02：状态序列化与比较器

- 状态：DONE（待提交）
- 实现负责人：Huijun（Claude Agent Teams：dev-map / dev-toml2 / dev-fio / dev-norm / dev-diff 完成 Layer 1；dev-wire / dev-runner 完成 Layer 2；lead 完成注册表重定位与验收）
- 复核人：Huijun（自审 + 双路 Claude subagent 审查：rev-fortran / rev-python）
- 前置任务/接受证据：M2-01（`638a3ae`）、M1-03（`a47ec72`）
- 工作分支/起始提交：`main` / `638a3ae`
- 能力组合与明确排除项：static_2d 路径三个 covered 检查点；`restart_ready` 不导出；不做 M2-03 的冻结与扰动运行；不改算法/数据结构/输出格式

## 问题与预期行为

M2-01 只有映射表，没有取值手段。预期：在三个锚点导出 190 个字段，规范化为 `docs/01 §5` 的九文件加指纹，比较器结构优先并把差异定位到对象与字段；导出关闭时二进制行为与结果逐字节不变。

## 变更边界

- 新增 `src/state/{yl_state_io,yl_state_adapters,yl_state_dump}.f90`（第三个为生成文件）、`tools/{yl_state,yl_state_diff}.py`、`docs/m2/M2-02-state-serializer.md`、`docs/m2/evidence/{S02-negatives,state-fingerprints}.json`、本文件
- 修改 `src/diagnostics/yl_diag.f90`（`--dump-state`，与 `--check-legacy` 互斥）、`legacy/yl/Fem.f90`（1 行 `use` + 3 行守卫调用）、`tools/build.sh`（编译序）、`tools/yl_run.py` + `schemas/run-manifest.schema.json`（`state` 块）、`tools/yl_state_map.py`（`emit` 词表 + 3 条新规则 + `gen-fortran`）、`docs/m2/state-field-map.toml`（`emit` 键、5 行修正）、`docs/m2/state-field-map.md`
- 因行号漂移重定位：`docs/m1/{reader-inventory.toml,io-sites.json,evidence/**}`、`src/diagnostics/yl_diag_registry.f90`
- 禁止：`cases/golden/*/reference` 任何改动

## 验证计划与实际结果

| 测试 ID | 内容 | 判据 | 结果 |
|---|---|---|---|
| B01 | release/debug/strict 构建 | 0 warning | PASS |
| N-off | dump 关 × 3 profile × 2 例 | `yl_compare` max\|d\|=0 | 6/6 PASS |
| N-on | dump 开 × 3 profile × 2 例 | COMPLETED；max\|d\|=0；`1.flavia.res` 与 dump 关逐字节相同 | 6/6 PASS |
| S01 | release × 2 例各 3 次 | 指纹相同 | PASS |
| S01b | `yl_state_diff` release vs debug | PASS compared=190 | PASS |
| S02 | 九类反例（真实快照） | 全部 FAIL 且定位到字段 | 9/9 PASS |
| S02b | 比较器护栏四类（ragged 长度/重组、reference 缺检查点、两侧缺字段） | 全部 FAIL 且定位 | 4/4 PASS（Round 1 修复后新增） |
| I03 | 探针 F01–F34 + N01–N18 | 47/47 | PASS |
| check-legacy | 3 profile × 2 例 | CHECKED | 6/6 PASS |
| 互斥 | 两种参数顺序 | PARSE exit 2 | PASS |
| 注册表 | M1 check / wrap dry-run / M2 check | PASS / 0 edits / PASS | PASS |
| 证据重采 | `yl_io_trace.sh` 两例 | 211 位点、767/367 命中不变 | PASS |
| 自测 | map 88/88、normalize 42/42、diff 9 类 12 检查 | 全通过 | PASS |
| 数值门 | 结果不变 | 见 N-off / N-on | PASS |

## 完成标准与回退

上表全部 PASS 且复核无 Critical。回退：`git revert` 本任务提交；reference 与 golden 不受影响。

## 偏离与未决

- 指纹跨 profile 不可比（派生高斯浮点末位差异，映射表已按 `abs_tol` 登记）；跨构建等价性由比较器判定。登记为 R26。
- `nextr` 无持久留存，快照无法证明外推等价（R25）。
- `mesh.sets.elset` 的 `shape`/`index_by` 在实测后修正为按 elset 分组（新增 shape 记号 `nelgroup`）。
- mass 积分规则的两条 ignore 行 consumers 填生产者 `read_element`，因路径上无真实消费者。
- 复核人未指定，阶段停在 IN_REVIEW。

## 复核记录（2026-09-07）

- 质量门：verify-change 通过（2 条 INFO 已处理）、verify-quality 0/0、verify-security 0。
- rev-fortran（实际构建并运行复核）：无 Critical/High。Medium 一条（`M2-01-checkpoints.md` 行号未随插桩更新）已按 census 推导的映射整体修正（374 处零失配）并加说明段。Low 四条记录不改：字段计数按连续同 id 段计、`yl_diag` 路径过长分支不可达、目录名末尾空格被 `trim` 丢弃、`first_in_set` 对 `ndofix` 二次复杂度。
- rev-python：**Critical 一条**——`yl_state_diff.py` 对 ragged 字段按扁平位置比较，导致"集合少一个节点"或"节点跨集合迁移"判 PASS（lead 已复现）。**High 一条**——reference 侧缺整个检查点被跳过。**Medium 一条**——两侧都缺的字段永不报错。三条均已修复并各加护栏用例；修复后 selftest `s02=9 guards=4 checks=16`，真实快照自比与重复对比仍 PASS compared=190，无假阳性。Low 一条（互斥退出码 3 与计划文本"1"不符）系计划笔误，已改计划。

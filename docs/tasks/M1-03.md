# 任务 M1-03：数量/引用/分配保护、负向探针、R17/R18 修复、裸 `stop` 替换

- 状态：DONE（待提交）
- 实现负责人：Huijun（Claude Agent Teams：dev-diag2 / dev-tools / dev-probes 完成 Layer 1；lead 直接完成 Layer 2/3）
- 复核人：待指定
- 前置任务/接受证据：M1-02（`8880b9a`）
- 工作分支/起始提交：`main` / `8880b9a`
- 能力组合与明确排除项：static_2d 路径已执行 reader 的消费点；路径上 38 处裸 `stop`（39 条语句，`Temper.f90:455` 一处两条）。不含路径外例程、不含 `material_serial` 解析、不含乱序 id 支持

## 问题与预期行为

M1-02 后坏文件/EOF/语法错已结构化，但语义坏值仍会走到分配或索引：`npoin=-5` 直接 `allocate` 失败（rc 非零、无报文），
`.ele` 引用节点 300 会用 `coord(:,300)` 越界，`.pre` 的 `itcurve=0` 读 `tcurves(0)`（R18），一维单元类型在 `kinddefine`
里用未初始化 `t/u`（R17），路径上的裸 `stop` 以 rc=0 结束（R20）。预期：坏数量在首个 allocate 前、坏引用在消费前被
`HSTAR_DIAG` 报文拒绝（RANGE/REF/DUPLICATE exit 2、UNSUPPORTED exit 3，报文含 reader、字段、违规值、允许范围）；
两例三 profile 结果逐值不变。

## 变更边界

- 修改 `legacy/yl` 10 个文件（守卫、R17/R18、38 处（39 条）`stop` 替换、`Residu.f90` 加 `use yl_diag`、`Prescrib.f90` 加 `use applied_load, only: ntcurve`）、`src/diagnostics/yl_diag.f90`（新 API）、`src/diagnostics/yl_diag_registry.f90`（重生成）、`tools/{yl_probe,yl_run,yl_wrap_reads}.py`、`schemas/run-manifest.schema.json`、`docs/m1/{reader-inventory.toml,io-sites.json,evidence/**}`、`legacy/source-manifest.json`
- 新增 `cases/probes/failure/N01–N18`、`docs/m1/M1-03-semantic-guards.md`
- 禁止：算法、数据结构、输出格式改动；不动注册表位点的 read/check 语句本身；路径外例程不动

## 验证计划与实际结果

| 测试 ID | 内容 | 判据 | 结果 |
|---|---|---|---|
| B01 | release/debug/strict/trace 构建 | 0 warning，manifest 门通过 | PASS |
| N-invariance | release × 两例 | COMPLETED；`yl_compare` max\|d\|=0 | PASS |
| C-debug | debug × 两例 | COMPLETED；max\|d\|=0（R18 关闭证据） | PASS |
| C-strict | strict × 两例 | COMPLETED；max\|d\|=0（R17 关闭证据）；无新未初始化点 | PASS |
| check-legacy | 三 profile × 两例 `--check-legacy` | CHECKED | 6/6 PASS |
| I02 | F01–F34 回归 | 与 M1-02 相同 | 29/29 PASS |
| I03 | N01–N18 语义探针 | status/code/reader/field/value/index/diag_count/indices 命中 | 18/18 PASS |
| M1-01 一致性 | `scan`、`yl_io_trace.sh` 两例、`check` | 211 位点命中数不变，输出与 reference 逐字节相同；`check` PASS，wrapped 152/152；`yl_wrap_reads.py --dry-run` 0 edits | PASS |
| 质量门 / 双模型审查 | verify-change / verify-quality / 双路 Claude subagent 审查（codex MCP 不可用） | 无 Critical | PASS（Medium×2 已登记，见下） |

## 完成标准与回退

上表全部 PASS 且复核通过。回退：`git revert` 本任务提交；reference 目录不受影响。

## 偏离与未决

- `.loa` 曲线 `ntime` 按 ≥0 守卫而非计划的 ≥2：HARMONIC/SEISMIC 等类型不用点表，ntime=0 是合法输入。
- `.pre` 集合头的守卫全部立即失败（计划为 ifixvar/itcurve 累积）：同一集合的消费紧随其后，累积需要跳过消费逻辑，收益小于风险；`list_fix` 仍按集合累积。
- `.ele` 坏记录采用"只扫描"模式而非"钳位后继续"：钳位会造成退化几何，strict 下可能在 flush 前触发 FPE。
- N16 需要"缺曲线"REF 成为首条报文，故该检查放在 ifixvar 范围检查之前（两条都会报）。
- Fem.f90 `'no such restart_ctt!!'` 同文本 5 处，只替换路径上的 3943 行那一处。
- `material_serial` 与 imat 一致性未做（title reader 不解析序号）。
- 守卫插入使 166 个注册表位点行号漂移，已按 `docs/m1/M1-03-semantic-guards.md`"注册表维护"节重定位；`diag_check_open` 的 site 字面量随之重写 13 处。
- 复核（2026-09-07，Claude 两路 subagent 审查 + verify-change/verify-quality；codex MCP 本会话不可用）：无 Critical/High。Medium 两条已记入 `docs/m1/M1-03-semantic-guards.md`：(1) 三处行内 `if(...) stop` 漏审，留 R20；(2) `.mat` mmats=nmats 守卫严于必要，登记为契约变更。Low：nstep=0 no-op deck 被拒、itcurve=0 因子恒 1、空参数报 PARSE、只扫描模式依赖单 read——均已登记。
- 复核人：Huijun（自审 + subagent）。

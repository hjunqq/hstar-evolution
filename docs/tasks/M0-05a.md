# 任务 M0-05a：两例模型说明、观察量与容差登记

- 状态：IN_REVIEW
- 实现负责人：Huijun（Claude 辅助）
- 复核人：待指定
- 前置任务/接受证据：M0-01（`141d31c`）
- 工作分支/起始提交：`main` / `141d31c`
- 能力组合与明确排除项：只读 deck 与 reader 源码核实模型；不修改输入；不作物理正确性结论

## 问题与预期行为

算例名称不能证明物理模型（R08）。预期：每例有 MODEL.md 说明网格、公式、材料、约束、荷载、
分析步与输出；观察量与容差在查看结果前登记。

## 变更边界

新增 `cases/golden/static_2d/*/{MODEL.md,observables.toml,tolerances.toml}`。
核实依据：`.cor/.ele` 几何统计（Python）、`Prescrib.f90:214-221` 的 `.pre` 记录格式、
`Load.f90:893-901` 的体力记录 `gravy,factg,factf`、`Fem.f90:2405-2406` 的 `.man` 增量记录。

## 验证计划

| 测试 ID | 输入/模式 | 判据 | 预期关系 | 证据位置 |
|---|---|---|---|---|
| M0-05a-a | 几何统计 | 节点/单元数与 `.glb` NPOIN/NELEM 一致；无负面积 | 一致 | 本文件 |
| M0-05a-b | `.pre` 解读 | 约束集自由度码与节点位置对应 | Cook 左边全固定；Lamé 两对称边法向约束 | MODEL.md |
| M0-05a-c | TOML 语法 | `tomllib` 解析通过 | PASS | 本文件 |
| 数值门 | — | 不适用（登记任务） | NOT_APPLICABLE | — |

## 完成标准与回退

MODEL.md 覆盖 `docs/06` 要求的网格、平面假设、单位、荷载、约束；观察量含必需性、单位、尺度；
容差含 B02 精确相等与跨路径临时值及其依据。回退：删除三类文件。

## 实际结果

- 2026-09-07：两例几何统计通过（289/256、81/64；面积全部为正；Cook 总面积 1440，
  Lamé 四分之一环 2.3411）；`.pre` 解读：Cook 左边 17 节点 ux=uy=0，Lamé y=0 边 uy=0、
  x=0 边 ux=0；两例荷载相同，仅重力 9.81 m/s² 沿 −y；TOML 解析通过。
- 结论：两例均为重力荷载回归例；经典 Cook 与 Lamé 解析判据不适用（R08 已证实）。
- 观察量登记后，冒烟运行（M0-02 验证）核实了 `1.flavia.res` 的块结构：DISPLACEMENT（2 分量）
  与 STRESS（SIGXX/SIGYY/SIGXY/SIGZZ）；已回填 observables.toml 的分量列表，未更改任何容差。
- 未覆盖：跨路径容差为临时值，需 M0-04b 重复噪声数据后另建任务定稿。

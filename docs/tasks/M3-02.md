# 任务 M3-02：normalize / validate / capability gate / finalize + manifest

- 状态：DONE（待提交）
- 实现负责人：Huijun（Claude Agent Teams：dev-err / dev-builder / dev-manifest 完成 Layer 1；dev-pipeline / dev-ptest / dev-build4 完成 Layer 2；dev-pcheck 修 M3-01 门禁作用域；lead 裁定与验收）
- 复核人：Huijun（自审 + Claude subagent 审查 rev-m302）
- 前置任务/接受证据：M3-01（`802ee7f`、`962f7ca`）
- 工作分支/起始提交：`main` / `962f7ca`
- 能力组合与明确排除项：仅 `static-q4/1`（Q4 平面应变、线弹性、PROFILE、重力、单步单增量）；**不含** parse/reader、`build_runtime`、`commit_legacy_globals`；5 条无反例的规则明确不实现

## 问题与预期行为

M3-01 只有类型，没有把 draft 变成可信 ProblemState 的过程。预期：normalize→validate→capability gate→finalize 四阶段，
阶段间失败即止、阶段内累积全部缺陷，失败不留半成品，派生值与默认值全部可溯源。

## 变更边界

- 新增 `src/problem/{yl_problem_errors,yl_problem_profile,yl_problem_manifest,yl_problem_builder,yl_problem_pipeline,yl_problem_pipeline_selftest}.f90`、`docs/m3/M3-02-pipeline.md`、本文件
- 修改 `tools/build.sh`（`problem-types` 目标纳入新模块并运行两套自检）、`tools/yl_problem_check.py`（作用域修正 + 常设断言）、STATUS/05
- 禁止：`legacy/yl`、`src/state/*`、`src/diagnostics/*`、`cases/golden/*`、M2 映射表参与快照的键、求解器链接链

## 验证计划与实际结果

| 测试 ID | 内容 | 判据 | 结果 |
|---|---|---|---|
| P-ok | 两例等价 draft 走完四阶段 | 成功；manifest 覆盖全部派生值、默认值与 8 条声明数量核对（条目数随集合数变化：两个约束集合的 cooks draft 为 16 条） | PASS |
| P-neg | 反例矩阵 | 每条已实现规则的**每个条件**至少一个反例触发且定位正确 | PASS。能力表 15 行逐行覆盖并由套件断言；validate 家族补齐 8 条规则的多分支（新增 170 项） |
| P-I04 | 三态 | unset / explicit empty / authored zero 判定互不混淆 | PASS |
| P-T01 | 事务性 | 失败后 draft 逐位不变、无产物、无 manifest；随后合法 draft 成功 | PASS |
| P-stage | 阶段屏障 | 前阶段失败时后续阶段不执行 | PASS |
| P-acc | 累积 | 单阶段三个独立缺陷全部报出 | PASS |
| P-self | 两套自检 | 类型 50/50、流水线 **485/485**（473 规则 + 2 覆盖守卫 + 10 兜底网，后者不计入规则覆盖），能力表行覆盖 15/15 | PASS（release + strict）；计数与哈希冻结于 `docs/m3/evidence/M3-02-selftest.json` |
| P-build | `build.sh problem-types` | 0 warning，两套自检分别报告 | PASS |
| P-drift | 求解器 GNU build-id | `b10f13ba5694…` 不变 | PASS |
| P-reg | M3-01 门禁 51/51、M2 映射表、M1 注册表 152/152、源码 manifest、探针 47/47、状态探针 4/4、两例 `yl_compare` | 不变 | PASS，max\|d\|=0 |

## 完成标准与回退

上表全部 PASS 且复核无 Critical。回退：`git revert` 本任务提交；legacy、冻结基线与 M2 证据均未改动。

## 偏离与未决

- **finalize 规模更正**：计划写 10 行自相矛盾——被禁止的 `cdofn`/`lcdofn`/`ndofix` 恰是第 7 条 index_map 与那 2 条 count。实际 6 条 index_map + 1 条 default。由 dev-pipeline 发现，lead 复核后更正计划。
- **`declared_counts_t` 为新增公开类型**：deck 的冗余数量声明在 legacy 形状的 `problem_state_t` 里没有落点，改为可选尾参传入，"未声明"与"声明为零"保持可区分。dev-pipeline 与 dev-ptest 独立得出同一形状。
- **新增规则 P0**：`profile_in` 与本次构建的 profile tag 不符即报错。被忽略的选择器比没有选择器更糟。
- **序号 0 按 legacy 的"none"接受**（重力与边界 amplitude），这正是 V14 被剔除的那个缺口；真正的修复要等 adapter 层能区分。
- 5 条规则因无可达反例不实现，见 `docs/m3/M3-02-pipeline.md` §4。
- 复核人未指定，阶段停在 IN_REVIEW。

## 复核记录（2026-09-08）

- 质量门：verify-security 0/0/0/0。
- rev-m302：无 Critical，**一条 High**。已复核：把 `zero_only` 判据改成 `v >= 0` 后套件掉到 `FAIL: 407/457` 并点名全部 10 条 V22 分支，证明套件确实会咬；T01 用**比套件更强**的姿势复核（在已发布的一对产物上逐阶段注入失败，确认先前产物仍分配且逐位不变、manifest 未增条目）；三态、范围（五模块无 IO/parse/RuntimeState/legacy 全局）、全部回归证据均复现。
- **High（已修）**：`derive_node_sets` 按边界序号最大值定集合数，序号有缺口时发布零长 `mesh%nsets` 条目——I04 破口，且证伪 V21。修法为**拒绝而非填补**（新增 V24）。
- **修 High 时由结构性守卫牵出另外两个同源原因**：(1) 元素集合按 id 引用元素却从无规则校验这些 id 是否存在（V4/V5 只覆盖节点），补 **V25**——这是整份反例矩阵漏掉的覆盖漏洞；(2) 越界 `element%elset` 使输入错以 exit 6 内部故障形式暴露，改为映射判坏即停止后续派生。
- **Medium（已修）**：文档写死"manifest 15 条"不可复现，实为 16 条（该 draft 有两个约束集合）。教训：记条目数必须写明针对哪个 draft。
- **Low（已修）**：三处文档计数滞后（201/201）。现统一为 481/481，并与源码 sha256 成对冻结于 `docs/m3/evidence/M3-02-selftest.json`。
- 残留（已登记非缺陷）：两个 section 且元素全指向第一个时仍可能造出空集合，今天不可达仅因能力门只允许一个 section。按常设决定不加无反例的规则，由 `INV-EMPTY-DERIVED` 兜底，并在能力表该行旁登记"section 数超过 1 时必须补规则"。

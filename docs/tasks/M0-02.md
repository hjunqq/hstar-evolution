# 任务 M0-02：Linux 首发构建说明及脚本，依赖清单

- 状态：IN_REVIEW
- 实现负责人：Huijun（Claude 辅助）
- 复核人：待指定
- 前置任务/接受证据：M0-01（`141d31c`）
- 工作分支/起始提交：`main` / `141d31c`
- 能力组合与明确排除项：只构建纯快照 `5414e73`；不修改源码；不做 Windows/gfortran

## 问题与预期行为

原仓库 `build_linux.sh` 依赖仓库外桩文件与 `mkl/latest` 符号链接，且未记录依赖哈希。
预期：干净检出在 `env -i` 下可构建，所有外部依赖有路径/版本/哈希，二进制可追溯到源码清单。

## 变更边界

新增 `tools/env.sh`、`tools/build.sh`、`docs/build-linux.md`、`docs/m0/evidence/*.stderr.txt`。
`build/` 由 `.gitignore` 排除，不提交二进制。

## 验证计划

| 测试 ID | 输入/模式 | 判据 | 预期关系 | 证据位置 |
|---|---|---|---|---|
| B01 | `env -i HOME PATH=/usr/bin:/bin bash tools/build.sh release` | 构建成功；manifest 无未解析依赖；`env -i ldd` 全部解析到 env.sh 固定目录 | PASS | 本文件 |
| B01-debug | 同上 `debug` | 构建成功 | PASS | 本文件 |
| 冒烟 | release 二进制在隔离目录运行两例（M0-03 之前的手工验证） | 退出 0、收敛、`1.flavia.res` 存在 | 两例 PASS | 本文件 |
| 交叉核对 | 冒烟输出 vs 来源仓库历史 `1.flavia.res` | 逐字节比较（非正式，来源构建未登记） | 相同 | 本文件 |
| 检查型 profile | debug/strict/sanitize 运行两例 | 完整运行 | 见实际结果 | `docs/m0/evidence/` |
| 数值门 | — | M0-04 才适用 | NOT_RUN | — |

## 完成标准与回退

`env -i` 构建通过且 manifest 完整；文档记录依赖、profile 与已知阻塞。回退：删除 `tools/` 脚本与 `build/`。

## 实际结果

- 2026-09-07 B01 PASS：release 构建 2 min 17 s，0 warning/remark，二进制 SHA-256 `6df6ea8d18c9fa5d…`，
  12 个运行时依赖全部解析（MKL 2026.1、ifx 2025.3 运行时）。首次尝试因 RUNPATH 不传递到
  MKL 的 `libintlc.so.5` 而失败（manifest 正确拒绝，退出码 5），改为 `-Wl,--disable-new-dtags`
  生成 RPATH 后通过；`env.sh` 同时清除继承的 `LD_LIBRARY_PATH`，否则会被其他 oneAPI 版本覆盖。
- debug/strict/sanitize 均可构建（约 35 s）。
- 冒烟 PASS：release 二进制在隔离目录运行 cooks_membrane（<1 s，2 次迭代收敛，峰值 RSS 27 MB）
  与 lame_cylinder（<1 s，3 次迭代）均退出 0；`1.flavia.res` 各 584 / 168 行，DISPLACEMENT（2 分量）
  与 STRESS（SIGXX/SIGYY/SIGXY/SIGZZ）块完整；`1.flavia.msh` 为 0 字节。
- 交叉核对：两例 `1.flavia.res` 与来源仓库 `/home/huijun/HSTAR_Next/cases` 中 2026-09-06 生成的
  历史输出逐字节相同（该历史输出的构建未登记，仅作非正式证据）。
- 检查型 profile 在纯快照上均未完整运行：debug 于 `Fem.f90:12288` 越界读 `tcurves(0)`（R18）；
  strict 于 `Elements.f90:2588` 使用未初始化 `t/u`（R17）；sanitize 被未插桩 OpenMP 运行时阻塞
  （工具链限制）。三者的 stderr 已存入 `docs/m0/evidence/`。这些不是 M0 出口条件，已登记为 M1-03 目标。
- 未覆盖：CI NOT_RUN；Windows/gfortran 未验证；MKL 内部无检查。

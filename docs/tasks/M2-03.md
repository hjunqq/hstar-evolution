# 任务 M2-03：两例状态证据与单字段扰动

- 状态：DONE（待提交）
- 实现负责人：Huijun（Claude Agent Teams：dev-probe / dev-specs 完成 Layer 1；lead 完成证据、冻结与验收）
- 复核人：Huijun（自审 + Claude subagent 审查 rev-m203）
- 前置任务/接受证据：M2-02（`4448d9c`）、M2-01（`638a3ae`）
- 工作分支/起始提交：`main` / `4448d9c`
- 能力组合与明确排除项：static_2d 两例；扰动覆盖材料 E、一个约束值、重力大小；不改 `legacy/yl`、`src/state/*`、`tools/yl_probe.py`；`restart_ready` 与 79 个 ignore 字段不在范围

## 问题与预期行为

M2-02 交付了导出与比较能力，但没有归档证据，也没有把"单字段扰动能被准确定位"变成可复跑的测试。
预期：两例各三次运行摘要一致；改一个材料/约束/荷载各一次，diff 指出准确对象与字段；状态基线冻结供 M3/M4 比对。

## 变更边界

- 新增 `tools/yl_state_probe.py`、`cases/probes/state/{S03_E_cooks,S03_BC_cooks,S03_G_cooks,S03_E_lame}/probe.toml` 与 README、`docs/m2/M2-03-state-evidence.md`、`docs/m2/evidence/{repeat-state-*.json,S03-report.json,S03-diffs/*}`、`cases/golden/static_2d/*/reference/state/**` 与 `reference/frozen.json`、本文件
- 修改 `docs/STATUS.md`、`docs/05-execution-backlog.md`、`tools/README.md`
- 禁止：改动 `reference/` 下既有文件（`results.json`、`run-manifest-*.json`、`repeat-report.json`、`build-manifest.json` 均未变）

## 验证计划与实际结果

| 测试 ID | 内容 | 判据 | 结果 |
|---|---|---|---|
| S01 | 两例 × 3 次 release dump-on | 指纹相同；3 对两两 diff PASS；162/11/17 | PASS（cooks `547e53a8fc6c`、lame `7c2edf39a342`） |
| S03-1 | `S03_E_cooks` | exit 1，命中 `materials.E` / `materials[1].E` | PASS |
| S03-2 | `S03_E_lame` | 同上（lame） | PASS |
| S03-3 | `S03_BC_cooks` | 命中 `steps0.boundary.value`，`count=1/34`，R24 两字段全零且未出现 | PASS |
| S03-4 | `S03_G_cooks` | 命中 `steps0.load.gravity.magnitude` | PASS |
| 非干扰 | dump 开/关 × 两例 | `1.flavia.res` 逐字节相同；`yl_compare` max\|d\|=0 | PASS |
| 冻结 | `verify` 两例；重复 freeze | files=40 PASS；重复冻结被拒绝 | PASS |
| 自检 | `yl_state_probe.py --selftest` | 空扰动判失败等 47 项 | 47/47 PASS |
| 回归 | 探针 47/47、`--check-legacy` 6/6 | 不变 | PASS |
| 数值门 | 不改源码 | 见非干扰 | PASS |

## 完成标准与回退

上表全部 PASS 且复核无 Critical。回退：`git revert` 本任务提交；冻结树随提交一并回退，`reference/` 既有文件不受影响。

## 偏离与未决

- `forbid_fields` 的全零要求改为按基线判定：基线全零则扰动后必须仍全零，基线非零（`materials.density`、`gravity.direction`）只要求不出现在 finding 中。无条件全零会误判三条探针。
- `[expect_state]` 不含 `unit`/`source`/`legacy`：比较器的 JSON finding 不导出这三个键，它们只在渲染文本里；且三者均来自映射表，复制进探针会多出一份可漂移的副本。
- `original_token` 放在 `[[derive]]` 内（规格如此），工具同时接受顶层写法，两者并存时必须一致。
- 复核人未指定，阶段停在 IN_REVIEW。

## 复核记录（2026-09-07）

- 质量门：verify-quality 0/0、verify-security 0、verify-change 1 条 INFO。
- rev-m203：无 Critical/High。全部头条数字独立复现（含四份归档 diff 文本逐字节一致）；冻结与校验在重复冻结、目标符号链接、路径穿越、非 COMPLETED 来源等攻击下全部拒绝，verify 能检出单字节翻转与增删文件；仓库无任何位置把 `yl_state.normalize` 指向 `reference/state`。
- Medium 两条已修：(1) `forbid_fields` 可写"空转"条目（禁用一个从不导出的字段等于什么都没断言，且报告记 `all_zero: true`）→ 加载期拒绝 `ignore` / `emit=none` / 不在基线中的字段，`exported` 为假时 `all_zero` 记 null；(2) 探针 README 两条命令与实际不符 → 已改。
- Low 两条已修：`freeze` 原先只信任 run manifest 的 `case_id` → 增加按树内在特征（节点/单元数 vs `observables.toml`）的交叉校验，读数记入 `frozen.json` 的 `case_identity`；证据 JSON 的路径 scrub 约定未记录 → 已在 `M2-03-state-evidence.md` §6 写明与 `frozen.json` 保留绝对路径的理由。
- 修复后：selftest 40→47，四条探针重跑仍 4/4 PASS；基线按新契约重新冻结，树内容与原先逐字节相同（`fingerprint.json` sha 未变），新增 `case_identity`。

# 任务 M0-01：源码/算例来源清单与 SHA-256；历史与当前修复差异表

- 状态：IN_REVIEW
- 实现负责人：Huijun（Claude 辅助）
- 复核人：待指定（单人项目，阶段停在 IN_REVIEW）
- 前置任务/接受证据：无；ADR-0003 与文档修正已提交（`b193279`）
- 工作分支/起始提交：`main` / `b193279`
- 能力组合与明确排除项：只登记来源与哈希，不构建、不运行、不修改任何源码或算例输入

## 问题与预期行为

导入的 `legacy/yl` 与两个种子算例此前没有逐文件哈希，也没有说明原仓库工作树里 389 行
未提交修改与快照的关系。预期：任何后续结果都能追溯到固定字节；脏树差异逐 hunk 有分类。

## 变更边界

- 新增 `tools/yl_manifest.py`（Python 标准库，`generate` / `check`，fail-closed）；
- 新增 `legacy/source-manifest.json`、`cases/golden/static_2d/*/input-manifest.json`；
- 复制 `gidpost_stub.c` 到 `legacy/stubs/`（来源 `_archive/src`，SHA-256
  `c87eb19a72eb1e3fec9d0eefa03bc956f46ca83c9eb28dc0773071a611f41fd6`，覆盖 gidpost.F90 全部
  126 个 BIND(C) 符号，链接用空实现，不产生 GiD 二进制输出）；
- 新增 `docs/m0/orig-worktree-diff.md`；
- 禁止：改动 `legacy/yl`、算例输入、求解算法。

## 验证计划

| 测试 ID | 输入/模式 | 判据与容差依据 | 预期关系 | 证据位置 |
|---|---|---|---|---|
| M0-01-a | `yl_manifest.py check` 三个清单 | 逐文件存在、字节数、SHA-256 相同；无未登记文件 | PASS | 本文件“实际结果” |
| M0-01-b | 篡改副本（改 `.glb`、加 `.chk`、删 `.tem`） | 必须报 HASH/SIZE、UNLISTED、MISSING 并非零退出 | 退出码 1 | 同上 |
| M0-01-c | 导入输入 vs 来源仓库 `d3e921c` 逐文件 `cmp` | 字节相同 | 全部 same | 同上 |
| M0-01-d | `1.LOA` / `1.loa` | 清单记录大小写冲突且 `identical_bytes=true`；程序只打开小写 | 记录后关闭 R04 | input-manifest.json |
| 数值门 | — | 纯清单任务，不适用 | NOT_APPLICABLE | — |

## 完成标准与回退

清单可由干净检出重新生成且哈希一致；篡改必失败；差异表覆盖脏树全部 hunk。
回退：删除新增文件即可，无状态副作用。

## 实际结果

- 2026-09-07：`legacy/` 39 个文件（含 yl 快照、Windows 工程附件与 stubs）、两例各 16 个输入文件已登记；`check` 三个清单 PASS；
  篡改副本退出码 1 并逐项报告；导入输入与来源仓库逐文件相同；来源仓库另含历史运行输出
  （`1.chk`、`1.flavia.res` 等）未导入，可在 M0-04a 作非正式对照。
- 脏树 15 组 hunk 已分类：EXCLUDE 3、DEFER 9、VERIFY 3（`Stiff.f90` 的 `factw` 初始化为重点）。
- 未覆盖项：来源仓库 `hstarYLOrig` 的 `.git` 历史未镜像；编码转换（R05）未开始。

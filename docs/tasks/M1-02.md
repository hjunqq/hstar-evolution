# 任务 M1-02：结构化 Error、checked I/O、退出协议

- 状态：IN_REVIEW
- 实现负责人：Huijun（Claude Agent Teams：dev-diag / dev-runner / dev-probe / dev-wrap，lead 复核）
- 复核人：待指定（codex 与独立 Claude 审查员报告见 `.ccg/tasks/m1-02-checked-io/review.md`）
- 前置任务/接受证据：M1-01（`18c7efd`）
- 工作分支/起始提交：`main` / `18c7efd`
- 能力组合与明确排除项：static_2d 路径 152 个注册 reader + 14 个输入 open；不含语义检查、R17/R18、裸 `stop` 替换

## 问题与预期行为

坏输入此前表现为运行时崩溃（缺 `1.cor` → rc=24，无报文）或静默读入垃圾。预期：缺文件、EOF、
语法错误以 `HSTAR_DIAG` 单行报文 + 退出码 2 结束，报文含文件、reader ID、记录序号、字段与循环索引；
正常两例结果逐字节不变。

## 变更边界

- 新增 `src/diagnostics/yl_diag.f90`、`yl_diag_registry.f90`（生成）、`tools/yl_wrap_reads.py`、`tools/yl_probe.py`、
  `cases/probes/failure/**`、`docs/m1/M1-02-checked-io.md`
- 修改 `legacy/yl` 10 个文件（178 处：152 read、14 open、10 处 `use`、2 处钩子）、`tools/{build.sh,yl_run.py,yl_io_inventory.py}`、
  `schemas/run-manifest.schema.json`、`docs/m1/{reader-inventory.toml,io-sites.json,evidence/**}`、`legacy/source-manifest.json`
- 禁止：算法、数据结构、输出格式改动

## 验证计划与实际结果

| 测试 ID | 内容 | 判据 | 结果 |
|---|---|---|---|
| B01 | `env -i` release/trace 构建 | 0 warning | PASS |
| N-invariance | 两例 release 运行 | COMPLETED；`yl_compare` max\|d\|=0；`1.flavia.res` SHA 与 reference 相同 | PASS |
| M1-01 一致性 | `scan` 计数；`yl_io_trace.sh` 两例 | I/O 语句数不变；211 位点按 ID 计数不变；`check` PASS，wrapped 152/152 | PASS |
| I02 | 探针 F01–F14（缺文件）、F20/F22/F23/F28–F32/F34（截断/清空）、F24–F27/F33（类型错） | INPUT_ERROR；code/stage/file/reader/index 命中 | 29/29 PASS |
| I02-blind | F21（删一个值） | EOF@289（静默跨行 + 整数接受实数形式） | PASS（登记 R19/R21） |
| check-legacy | 两例 `--check-legacy`；F24 物化目录 `--check-legacy` | CHECKED；INPUT_ERROR | PASS |
| 质量门 | verify-quality / verify-security / verify-change | 无 Critical | PASS（security 17 条为 CLI 路径误报） |
| 双模型审查 | codex + 独立 Claude，3 轮 | 无 Critical | Round 1：2C/6W 已修；Round 2：3W 已修；Round 3：无 C，3 条边角 W 留作后续 |

## 完成标准与回退

上表全部 PASS；文档与风险表更新。回退：`git revert` 本任务提交；reference 目录不受影响。

## 偏离与未决

- 单行 `if (c) read` 保留内联并追加 `if (c) call diag_check_read`，未改块 if（四处条件均为纯比较）。
- 模块头同时 `use yl_diag_registry`（`yl_diag` 默认 private，不转出 `RD_*` 常量）。
- 缺文件报文无 `reader=`（open 无 reader ID），以 `unit=`/`site=` 定位；探针不断言 reader。
- 注册表中 7 个 reader 的 `field` 超 256 字符被截断，仅影响报文显示。
- 后续项（不阻塞）：`yl_wrap_reads.py` 比对字符串字面量时不应小写化；`yl_run.py` 零条 READER 行时仍应比对计数；HSTAR_CHECK 摘要行严格引号解析。
- 复核人未指定，阶段停在 IN_REVIEW。

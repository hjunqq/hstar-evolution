# 任务 M2-01：检查点和状态字段映射表

- 状态：DONE（待提交）
- 实现负责人：Huijun（Claude Agent Teams：dev-tool / dev-toml / dev-doc 完成 Layer 1；lead 完成 Layer 2）
- 复核人：Huijun（自审 + 双路 Claude subagent 审查，codex/gemini 不可用）
- 前置任务/接受证据：M1-01（`18cf7ed`）、M0（`f7abe9d`）、M1-03（`a47ec72`）
- 工作分支/起始提交：`main` / `a47ec72`
- 能力组合与明确排除项：static_2d 路径（Q4 平面应变、线弹性、PROFILE、重力、单增量、restart=0）；只登记，不改 `legacy/yl`、不改 M1 注册表、不写序列化/插桩代码（M2-02）；`restart_ready` 登记为未覆盖

## 问题与预期行为

M2-02 的序列化器和比较器需要知道：在哪些位置（检查点）、哪些全局量是"已生效参数"、每个量来自哪个 reader 或派生规则、被谁消费、shape/dtype/单位、属于 ProblemState 的哪个对象还是 RuntimeState、以及怎么比较。没有这张表，S01–S03 的"定位到对象与字段"无法机器化。
预期：`docs/m2/state-field-map.toml` 逐字段登记；`tools/yl_state_map.py check` 证明 reader 覆盖完整、锚点位于首个消费者之前、词表闭合、符号在源码中存在；人读表由 `render` 生成。

## 变更边界

- 新增 `docs/m2/state-field-map.toml`（权威）、`docs/m2/state-field-map.md`（渲染）、`docs/m2/M2-01-checkpoints.md`（选址说明）、`tools/yl_state_map.py`、本文件
- 修改 `docs/STATUS.md`、`docs/05-execution-backlog.md`（M2-01 判据）、`docs/08-risk-register.md`（R23、R24）
- 禁止：`legacy/yl`、`docs/m1/reader-inventory.toml`、`cases/golden/*/reference` 任何改动

## 验证计划与实际结果

| 测试 ID | 输入/模式 | 判据 | 预期关系 | 结果 |
|---|---|---|---|---|
| M2-01-a | `python3 tools/yl_state_map.py check` | PASS；readers covered n/n（动态计算，当前 39） | PASS | PASS（`PASS: 269 fields, 4 checkpoints (3 covered), readers covered 39/39, skip-class 110`；0 条 WARN；Round 1 后 `%comp` 按类型链校验） |
| M2-01-b | `python3 tools/yl_state_map.py --selftest` | 每类规则至少一个坏样例 FAIL、一个好样例 PASS | 全部命中 | PASS（`SELFTEST PASS: 48/48`：1 好样例 + 46 坏样例 + render 幂等） |
| M2-01-c | 三个 covered 检查点 anchor hash | 与 `legacy/yl/Fem.f90` 当前行一致；规则 15（anchor 前无 `call <first_consumer>`）通过 | 一致 | PASS（1908=4f74a710eb3a、3601=d61e64d3a931、3654=83ef072c130e；规则 15 通过） |
| M2-01-d | `[[perturbation]]` 三条 | 指向 `ProblemState.*` 且 `exact`（E、约束集、重力） | 存在 | PASS（P-E `materials.E[1]`、P-BC `steps0.boundary.value[1]`、P-G `steps0.load.gravity.magnitude`） |
| M2-01-e | `python3 tools/yl_io_inventory.py check --evidence docs/m1/evidence/*/hits.json` | M1 注册表未动，仍 PASS | PASS | PASS（`wrapped readers: 152/152`） |
| M2-01-f | `render` 两次 | 字节相同；提交的 `.md` 与重新生成一致 | 相同 | PASS（两次 render `cmp` 相同，571 行） |
| 数值门 | — | 不改源码、不重建 | NOT_APPLICABLE | — |

## 完成标准与回退

上表全部 PASS，质量门与审查无 Critical。回退：`git revert` 本任务提交，不影响 reference 与 M1 注册表。

## 偏离与未决

- 需求初稿写"model_ready = 首个 `global_stif_profile`/`stiff_u` 之前"，字面读法会落到 `.man` reader 之后；实际锚点定在 `Fem.f90:1908`（`call solve` SET 之前），理由见 `docs/m2/M2-01-checkpoints.md` §3。
- 外部模型（codex 额度、gemini 无 key）不可用，构思/规划由两路独立 Claude subagent 完成。

## 复核记录（2026-09-07，Round 1 → Round 2）

- 质量门：verify-change 通过、verify-quality 0/0、verify-security 0。
- review-tool：无 Critical/High。Medium 两条已修：`%comp` 改为按声明类型逐级链走（`materials.props%e%nu` 现 FAIL）；`render` 改为 fail-closed（check FAIL 不写文件，`--force` 才写且仍 exit 1）。Low：缓存后 check 10 s → 0.4 s；去掉未用参数/死 flag。selftest 45 → 48。
- review-map：无 Critical/High。Medium 四条已修：路径守卫开关（block_stab、nbackf、ebody、ninit、uinitial、state_change、Bparameter、stab_matde）补为 exact 字段，restart/relis/nlayer/nlinks/ntrans/Qstatic/ADINA 由 ignore 改 exact；`derived.dof.active_flags` 与 `sections.material_header` 加 `reconstruct` 配方并理顺 derived_from；高斯点字段限定 egaus(1)，djacb 注明为 djacb×weigp，mass 规则单列 ignore；`steps0.activation.active` shape 含第 0 列（`nblks1`）。Low 全部处理（unode/lcdofn shape、三处行号、iblks/lblks 在 model_ready 补登、P-BC index 改 `[set=1]` 并核实 lame_cylinder 的 `.pre` 先列 set 2、tfixvar/nextr 补字段）。
- Round 2：`check` PASS 269 字段 0 WARN；render 幂等；M1 注册表 152/152 PASS。
- 判断项：mass 规则两条 ignore 行的 consumers 填 `read_element`（生产者），因路径上无真实消费者而工具要求非空。

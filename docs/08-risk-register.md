# 风险登记表

状态均为 OPEN，直到有关闭证据；本次为计划审查，不代表故障已经修复。

| ID | 证据/风险 | 影响 | 处置与关闭证据 |
|---|---|---|---|
| R01 | 历史源码提交与当前算例非同一时间基线，原工作树有修复 | 无法复现或保留已知错误 | M0 对照版本；单独导入必要修复；构建/运行可追溯 |
| R02 | 两例都声明 PROFILE，初稿却限定 PARDISO | 算法变更与输入迁移混在一起 | 已修计划；M0 复现 PROFILE，PARDISO 单独验证后关闭 |
| R03 | `Fem.f90` 多处阶段内 READ/REWIND | 启动摘要相同仍可能执行不同分析 | M1 路径清单、M2 多检查点、M4 延迟读取消除证据 |
| R04 | 同目录含 `1.LOA` 和 `1.loa` | Linux 与不区分大小写平台含义不同 | 已核实：两文件字节相同，`Global.f90:633` 只打开小写 `.loa`；保留原件并在 input manifest 记录，M0-01 记录后关闭，不需要冲突拒绝逻辑 |
| R05 | 原源码非统一 UTF-8 | 扫描丢失 reader、错误修改源码 | 按字节保全哈希；搜索使用二进制文本模式；编码转换独立任务 |
| R06 | legacy 包含 Windows 工程、预编译库及本地依赖 | 干净 Linux 构建失败、发布依赖来源不清 | M0 依赖/再分发范围清单与构建验证；不自动公开二进制 |
| R07 | 全局变量、指针别名、初始化副作用 | 新 bridge 产生悬空状态或污染参考 | 所有权表、独立进程差分、分配失败与再次加载测试 |
| R08 | Cook/Lamé 文件名未证明实际模型 | 错用解析公式，虚假物理通过 | 已证实：两例 `.loa/.mat/.man/.opr/.sol` 字节相同，仅重力体力；两例降为回归例，M5 新建解析 probe |
| R09 | 只比较哈希或最大值，数组截断可通过 | 错误被掩盖 | M2 结构化比较和缺失/截断/NaN/shape 反例 |
| R10 | 项目试图一次改完全部 READ/全局状态 | 首条闭环无限延期 | M1～M5 限定静力切片；长尾清单不阻塞首条独立能力 |
| R11 | bootstrap Schema 尚无材料/约束/荷载等 | 被误当可运行输入标准 | M5 替换为完整规范；此前明确不可用于运行能力声明 |
| R12 | golden 被运行输出或自动更新预期污染 | 基线丢失、自证正确 | 独立运行目录、前后哈希、基线变更独立评审 |

| R13 | 原仓库脏树《修改报告.md》与实际 diff 不符；Stiff.f90 含密度循环前 `factw` 未初始化修复 | 按报告归类补丁会漏掉影响重力路径的修复 | 已完成 M0-04c：candidate 两例与 reference 逐值相同，脏树对静力切片无影响；`factw` 初始化并入 M1-03 防御性修复。对其他能力切片开工时须重新对照 |
| R14 | 旧程序多处 `stop '文本'` 退出码为 0；`.chk` 与 stdout 含时间戳 | 仅凭退出码或字节比较误判成功 | M0-03 组合判据；比较只用解析后的 `1.flavia.res` |
| R15 | 种子算例关闭反力输出，且静力单增量流程是否调用 GiD 写出未经运行确认 | 首跑可能无可解析结果；反力平衡判据不可用 | 已确认 `1.flavia.res` 在 nincs=1 静力流程写出（M0-02/04a）；反力平衡仍需 M5 新建探针 |
| R17 | `Elements.f90:197` `kinddefine` 对一维单元类型不赋值 `t/u` 即调用 `shfunc`（`Elements.f90:2588`），使用未初始化实数 | 任何 `-init=snan`/`-fpe0` 运行在启动即中止；结果不受影响（相关项未用于线单元） | **CLOSED（M1-03，2026-09-07）**：每个积分点先 `t=u=0`；strict profile 两例 COMPLETED 且与 reference 精确相同（`docs/m1/M1-03-semantic-guards.md`） |
| R18 | `Fem.f90:12288` `modf_var_prescribed` 在约束集 `itcurve=0` 时读 `tcurves(0)%dfact/type_curve`，越界 | `-check bounds` 中止；release 下读到相邻内存，两例结果未受影响但不可保证 | **CLOSED（M1-03，2026-09-07）**：`itcurve==0` ⇒ `dfact=1`、`type_curve='NONE'`（`modf_var_prescribed` 与 `time_dependent`）；`.pre` 读取处校验 itcurve∈0..ntcurve 且 ifixvar=8/10 必须有曲线；debug profile 两例 COMPLETED 且精确相同 |
| R19 | list-directed READ 遇记录字段不足时静默跨到下一记录，`iostat=0`（ifx 实验与探针 F21 证实） | 少一个值的 deck 会整体错位而不报错 | M4 记录级 Legacy Adapter；M1-02 只保证 EOF/语法错可控 |
| R20 | 旧程序约 90 处裸 `stop`/`stop '文本'` 退出码为 0，未纳入退出协议 | 求解失败或内部错误仍可能以 rc=0 结束 | M1-03 已替换 static_2d 路径上的 38 处、39 条语句（审计表见 `docs/m1/M1-03-semantic-guards.md`：正常结束→exit 0，输入错→2，能力不支持→3，内部表→6）；路径上另有 3 处行内 `if(...) stop`（`Fem.f90:1902`、`Fem.f90:9336`、`Load.f90:1073`，复核发现，两例不触发）与路径外约 50 处保持 OPEN，运行器以组合判据兜底 |
| R22 | M1-03 起 `.cor`/`.ele` 的 id 必须等于行序、`.glb` Σnelgroup 必须等于 nelem、未知命令行参数报错（旧代码忽略 id、多余单元静默不读、只看首个参数） | 乱序 id 或多余单元的历史 deck 会被拒绝（RANGE/DUPLICATE），而旧程序能跑 | 契约变更登记于 `docs/m1/M1-03-semantic-guards.md`；两例 golden 满足；乱序 id 若有真实需求由 M4 Adapter 支持 |
| R21 | ifx list-directed 整数项接受实数形式并截断（`5.5`→5，`iostat=0`） | 输入错误被静默吞掉 | M4 记录级解析按字段类型严格判定；探针 F27 改用非数字 token |
| R23 | 派生类型含未初始化分量与未关联指针（`prescrib%ifixvar0` Prescrib.f90:274、`deltafi/delitfi` Fem.f90:246、`rvector` Solver.f90:7237、`element%field%rload/tload/eload`）；整体 dump 会把随机字节写进摘要 | 重复运行摘要不稳定，S01 假失败 | M2-01 在 `docs/m2/state-field-map.toml` 逐分量登记 determinism，未初始化/指针一律 `ignore`；M2-02 序列化器只按白名单分量输出，指针先 `associated()` |
| R24 | `fixed` 与 `tcurves%dfact` 在 `increment_ready(1,1)` 仍为 0（Fem.f90:3666-3667 才赋值） | 对 `fixed` 做 S03 约束扰动检不出差异 | M2-01 `[[perturbation]]` 把 S03 目标钉在 `props%…%e`、`prescrib%vdofix/nodfix`、`gravy/factg/tcurvegravity`；M2-03 按此执行 |
| R25 | `.pre` 集合头的 `nextr` 是 `prescrib_set` 局部量且无任何持久留存（`tfixvar` 经 `iffix=tfixvar+1` 留存，`nextr` 走 `Prescrib.f90:261-268` 另一分支分配 `listep/value_ext`） | 状态快照无法证明外推（extrapolation）行为等价 | M2-01 登记为 `ignore` 并在 `docs/m2/M2-02-state-serializer.md` §6 写明；两例 `nextr=0`，该分支不执行；若将来支持 `nextr/=0` 必须先补 reader capture |
| R26 | **（2026-09-09 扩写：原描述过窄）** 指纹不仅**跨 profile** 变，也**随 `state-field-map.toml` 的 sha256 变**——每个快照 json 携带 `map_sha256` 溯源键，映射表一改，冻结基线便不再逐字节复现（实测：冻结 `232f6bba…` vs 当前 `cef6838e…`，40 个文件全部字节不同，而**剥去该键后三个检查点的每一个字段值完全相同**）。原描述只写了前者：`runtime.gauss.gpcod/cartd/djacb` 是读入时计算的派生浮点，release(-O2) 与 debug/strict(-O0) 末位不同 | 误把指纹当跨构建等价判据会产生假失败 | 映射表按 `abs_tol` 登记这三个字段；跨构建等价由 `tools/yl_state_diff.py` 判定（实测 PASS compared=190），指纹只在同一二进制内、且映射表未变时做 S01 重复判据。**扩写部分的处置**：字节级同一性**从未被任何门禁强制过**——`reference/state` 的三个消费者（L3-a `yl_adapter_fidelity`、L3-b `yl_adapter_bridge_test`、`yl_map_selfcheck.py`）比较的都是**字段值**，且三处证据（95/98、`MATCH=64 MISMATCH=0`、47/47）全部完好并正是对这份基线测出的。`yl_state_probe.py verify` 报 PASS 不是缺陷：它自述是 `re-hash the frozen tree against frozen.json`，即**防篡改**检查，从未声称「新运行可复现」。**残留**：不存在「新运行 vs 冻结」的字节比对，谁需要它须新建；实质漂移由 L3-b 的值比对覆盖；见 `docs/m2/M2-02-state-serializer.md` §3 |
| R27 | **M1 的 gdb 证据是下界，不是测量值。** `tools/yl_io_inventory.py` 的 `gdb-script` 对每个站点只发一条 `break FILE:LINE`，**假设一行编译成一个地址**。ifx 会把一条数组段读取编译成**两段不相连的地址范围**、都标同一行号（实测 `Load.f90:231`：`0x5a9b16` 与 `0x5a9e38`），gdb 只绑定第一段，而执行进入第二段 —— 断点永不触发。失效随后**静默传播五步**：站点执行了 → 断点没打中 → 不在 `hits.json` → 未被加进 reader 清单 → 未被 `diag_check_read` 包装。`check` 发现不了，因为它校验清单与证据的一致性，**而两者都在同一个工具假设的下游** | **「两例各命中 211 位点」是下界，不是测量值，且目前无法知道低了多少。** 受影响的是任何以命中集为完备性依据的判断：M1-01 的 `not_on_path` 分类、M1-02 的「路径上读取全部包装」、以及将来任何「这条路径上再无未包装读取」式的论证。**不受影响、仍然可信的证据**：命中集内的 211 条是真命中（假阳性不是本缺陷的失效模式）；`check` 对清单与注册表的一致性校验；`wrap_reads --dry-run` 的 0 edits；两例数值逐值相同（`max|d|=0`）；M0/M2/M3 的全部证据（不依赖该工具） | **已关闭 2026-09-09**（`4f5c407`，证据 `docs/m1/M1-04-r27-closure.md`）：`gdb-script` 改为 dump gdb 自己的行表并对一行的**全部**地址下断点——1022 站点 → **5520** 地址，**643 个站点（63%）多于一个地址**，计数按地址取 max（max 逐条复现原有 211 个计数；求和会虚增其中 11 个）。**对差非空**：命中集 **211 → 216**。新增 `Load.f90:231`（2026-09-08 手工发现的那处，首次由机器测到：在 `0x5a9e38` 触发而旧脚本绑定的 `0x5a9b16` 始终沉默）与 `Output.f90:4301/4326/4351`、`Temper.f90:243`——后四处全部是 `goto` 跳入的带标号语句。类别边界不是「带标号」而是**「行表为一行给出的首地址不是执行进入的地址」**（`Load.f90:231` 无标号也无 goto，是 ifx 把数组段读取拆成两段不相邻范围）。四处已按 M1-02 流程包装，**分母 153 → 157**，`check` PASS、`wrap_reads --dry-run` 0 edits、manifest 重新生成并验证、EOF 反例以新 reader id 点名（旧二进制 rc=24 无任何诊断）。lead 独立复核：legacy 两文件无 U+FFFD 污染，且改后 release 重建在两个基准算例上 DISPLACEMENT/STRESS 均 `max|d|=0.000e+00`。**残留**：站点**集合**现为测量值；多地址站点的逐站**计数**仍是下界（报告 §6 用指令级对照给出界）。**原处置**：M1-01/M1-02 按本缺陷**有条件接受**（`Load.f90:231` 已修复验证，功能面无未决项）。**关闭条件**：`gdb-script` 改为对一行的**全部**地址下断点并合并计数，重跑两例 trace，用新命中集与现有 211 条对差；差为 0 则下界升格为测量值，差不为 0 则每条新站点按 M1-02 流程补包装。**关闭里程碑：M1-04**（若不单独立项，则并入 M4-02 之前必须完成——M4 的影子差分会以 M1 的完备性为前提）。见 `docs/m1/M1-finding-2026-09-08-unwrapped-loa-read.md` |
| R28 | **M0 签收后仍有两项判据只是断言，且判据 18 只满足一半。** 判据 6「七种隔离运行状态各有反例」只有 `docs/tasks/M0-03.md` 的文字叙述，假二进制套件与对应 run-manifest 从未持久化（`runs/` 整目录 gitignore）；判据 13「跨路径容差」的四组数字是量纲估算，`status="provisional"`，B02 三次重复逐字节相同因而**没有任何非零噪声观测**支撑；判据 18 的「负责人接受」已由 Huijun 于 2026-09-09 签收，但「独立于实现人的复核人读过 1～17 条证据」在单人项目里结构性缺席 | **签字不改变证据形态。** 风险是后续阶段把「M0 已 ACCEPTED」读成「M0 的 18 条都有独立证据」。具体影响：判据 6 无法区分「被审视后判为不适用」与「从未被看过」——与 R27 是同一个证据形态问题；判据 13 的容差一旦被 M4 的跨路径比对直接采用，等于用估算数当判据，会把噪声判成差异或把差异判成噪声。**仍然可信的**：M0 的 15 条证据支撑项、两例的确定性重复、构建与运行的可重现性 | **处置**：随 M0 签收登记，不阻塞后续阶段。**关闭条件**：判据 6——把假二进制反例套件固化为仓库内可重放脚本（或把七种反例的 run-manifest 摘录进 `docs/m0/evidence/`）；判据 13——**已关闭 2026-09-10**：按 ADR-0008 §4「先测噪声，不做成研究项目」，`tools/yl_noise.py` 以同一二进制、同一输入、同一环境重复 8 次 × 2 例、两两 56 对、**严格比较（atol=rtol=0）下 max|d| 处处为 0**（`docs/m4/evidence/noise/noise.json`）；阴性对照把一个位移分量改动 `1e-18` 即被报出，故这次的安静有意义。**观察不到噪声，因此不引入容差**：两例 `tolerances.toml` 的 `[cross_path]` 已定案为 `atol = rtol = 0`、`status = "measured"`。**限定**：这把**观测到的**噪声在该二进制/输入/环境/重复次数下界定为 0，**不是确定性证明**，也不涉及其他线程数或环境——那是另一个问题，要另测另标。原计划的「不同线程数/环境」一节**不再是本判据的前置**，若将来需要则单独立项；判据 18——出现第二个复核人时补齐，或如实长期记录为单人项目的结构性缺口。**关闭里程碑**：判据 13 必须先于 M4 的跨路径数值比对定案（否则 M4 会以估算容差作判据）；判据 6 并入 R27 的证据形态整改。见 `docs/m0/M0-acceptance-matrix.md` 签字页 |
| R29 | **没有任何门禁能验证「适配器覆盖了哪些读取站点」。** `docs/m1/reader-inventory.toml` 的 153 个站点 id 与 `src/adapter/**` 里的 `! RD:` 标记之间**没有机械核对**。两次独立统计给出不同的未覆盖数：lead 按「整个文件无解析器」算得 28（`.tem 9 / .ifs 8 / .opr 5 / .nrt 3 / .ftr 2` + `.loa` 1），dev-fold 按「id 未匹配上 RD 标记」算得 36；差异中至少一例（`amplitudes.points.value` ← `LOA.external_load_1.curve_factors`）是**已覆盖却被记为缺口**（`yl_adapter_load.f90:387` 的 RD 标记名为 `curve_points`，与 inventory id 对不上）。两个数都不是测量值 | **任何形如「适配器覆盖了全部读取站点」「这条路径上再无未解析的读取」的断言目前都无法证实，也无法证伪。** 与 R27 是同一形态问题（分类做在无法逐项核对的层级），但独立存在：即便 R27 关闭、命中集升格为测量值，仍然没有东西把命中集与解析器覆盖面对上。**仍然可信的**：`hits` 本身（153 个站点在两个算例上 hits 全部 ≥1，真命中）；以及本次两方独立算出并一致的结论——**落在 `model_ready` 比对面上且有真实比较规则的未覆盖行恰好 4 个，全部源自 `TEM.boundt.*`**，由 M4-01 的 `.tem` 解析器（步 4b）关闭 | **处置**：不阻塞 L2-c 折叠（折叠的判据是 map 行，不是站点覆盖率）。**关闭条件**：给 `! RD:` 标记与 inventory id 建立机械对照——标记直接写 inventory id（而非自造名），加一个门禁校验「每个 on-path 站点 id 恰好被一个 RD 标记引用，且每个 RD 标记引用一个真实 id」，纳入 `tools/build.sh adapter`。设计草案见 `docs/m4/L2c-fold-design.md` §1.9。**关闭里程碑**：与 R27 同批。**M4-02 于 2026-09-11 结束时仍未关闭**，且不再阻塞——M4-02 的判据是两条路径的**逐值相等**（`max|d| = 0`），不是覆盖率；覆盖面的断言仍然无法证实也无法证伪。关闭里程碑顺延至任何要以覆盖面作论据的验收。**已关闭 2026-09-13（M5 签收后的轻量 M1 复核）**：`tools/yl_coverage_check.py` 建立机械对照，接入 `tools/build.sh adapter`。六条断言（C1 标记必须解析到真实 id、C2 每个 on-path reader 要么被标记要么登记、C3 不得既标记又登记、C4 登记行的 `state_target` 必须与普查一致且必须给理由、C5 携带真实值的行必须另附**实测**证据、C6 每次运行按名字打印登记表），外加 9 例自检。**实测结果：on-path 154 = 已标记 133 + 登记未适配 21**（`.ifs` 8 / `.opr` 8 / `.nrt` 3 / `.ftr` 2，全部为 `title_skip`/`empty_section`，唯一携值行 `OPR.output_read.output_control` 附实测证据：两份 golden deck 五个标志全 0，`.opw/.oew/.ogw/.ojw` 在 legacy 与 modern 两条路径上**均为零字节**）。**R29 举的那个例子当场证实**：`Load.f90:231` 一直是覆盖的，只是标记与 `check_io` 的 id 都写成了 `curve_points`，应为 `curve_factors`——错误诊断会点错 reader，已修。另修：`.glb` 五个 title 用区间记法 `title#30..#34` 写标记，对门禁不可见，看上去像四个未适配，已逐条展开。**R29 原文给出的 28 / 36 两个数都不是测量值，现在有了测量值** | 
| R30 | **`PROV_VIA_RUNTIME` 条目里「值来自哪个 runtime 量」是散文，无机械对账。** 出处账本的 `FROM_RUNTIME` 集合等式（P5/P6）核对的是**哪些行**可以声称该状态，不核对**声称的那个来源是否被真的读取**。若有人把实现从「拷贝 `active_to_component`」改成「计算 `1..nfdof`」而不改标签，集合等式照样通过、字节照样相同（该二者在能力门允许的每个 deck 上是同一序列）、无门禁会响 | **标签会静默腐烂。** 与 `commit-inputs`（staging 读取 ⊆ 输入检查）是同一个问题，**只是 runtime 那一侧还没有对应物**——ProblemState 面与 residue 面都已有包含关系对账，runtime 面没有。影响面：任何以出处账本判断「这个值从哪来」的下游推理，包括影子差分的 `NOT_MIGRATED` 桶语义 | **处置**：M4-01 内不做——条目文本是散文，机械对账需解析它，代价与当时的收益不相称。**关闭条件**：把 `PROV_VIA_RUNTIME` 的来源字段从散文改为结构化（runtime 表达式），并按 `commit-inputs` 的形状加一条「staging 的 runtime 读取 ⊆ 账本声称的 runtime 来源」对账，配阳性对照。**关闭里程碑**：M4-02 之前，或任何要以出处账本作论据的验收之前。见 `docs/m4/L4-findings-backlog.md` 条目 J |
| R16 | 初稿 ProblemState 自创分类，与 CAE 惯例和 YL 对象都不对应 | 三套词汇并存，输入更混乱 | ADR-0003 采用 CAE 对象模型；docs/01、03 与 schema 已改，M3/M5 按其实施 |

每次新增风险补充责任人、发现提交、最小复现、预定处置阶段和关闭证据路径。
计划修正只解决文档问题，不能替代代码或运行验证。
